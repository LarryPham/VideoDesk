<resources>
    <!-- Application's Root Styles -->
    <style name="BaseVideoDesk.Theme" parent="Theme.AppCompat.Light" />
    <style name="BaseVideoDesk.Widget" parent="Theme.AppCompat"/>
    <!-- The Application Themes -->
    <style name="VideoDesk.Theme" parent="BaseVideoDesk.Theme">
        <item name="android:editTextBackground">@drawable/abc_edit_text_material</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

</resources>
